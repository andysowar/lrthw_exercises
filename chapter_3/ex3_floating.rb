# This is a string.
puts 'I will now count my chickens:'

# This outputs Hens and then the answer to the math.
puts "Hens #{25 + 30 / 6}"
# This outputs Roosters & then the answer to the math.
puts "Roosters #{100 - 25 * 3 % 4}"

# This is a string.
puts 'Now I will count the eggs:'

# This uses all of the different math operators and outputs the result.
puts 3 + 2 + 1 - 5 + 4 % 2 - 1.0 / 4 + 6
# This is a string.
puts 'Is it true the 3 + 2 < 5 - 7?'

# This uses math, but instead of giving a result it will give a true or false.
# This is because of the greater than sign.
puts 3 + 2 < 5 - 7

# This outputs What is 3 + 2? and then gives the answer using addition.
puts "What is 3 + 2? #{3 + 2}"
# This outputs What is 5 -7? and then gives the answer using subtraction.
puts "What is 5 - 7? #{5 - 7}"

# A string.
puts 'Oh, that\'s what it\'s false.'

# A string.
puts 'How about some more.'

# Outputs "Is it greater?" and gives a true or false answer.
# Using the greater than operator.
puts "Is it greater? #{5 > -2}"
# This will also give a true or false answer.
# Using the greater than or equal operator.
puts "Is it greater of equal? #{5 >= -2}"
# Same as above. This one use the less than or equal operator.
puts "Is it less or equal? #{5 <= -2}"
